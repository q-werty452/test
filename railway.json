{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn testcollege.wsgi --workers 4 --timeout 120",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
